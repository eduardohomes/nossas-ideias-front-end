import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComentarTarefaComponent } from './comentar-tarefa.component';

describe('CadastrarTarefaComponent', () => {
  let component: ComentarTarefaComponent;
  let fixture: ComponentFixture<ComentarTarefaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ComentarTarefaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    /*fixture = TestBed.createComponent(ComentarTarefaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();*/
  });

});
